%h1 Part Manufacturers

%div.container
  %div.layout-grid
    - @manufacturers.each do |manufacturer|
      %div.child
        %img{src: "/assets/logos/logo-" + manufacturer.name.downcase.gsub(' ', '-') + ".jpg", image_alt: 'manufacturer'}
        %h2= manufacturer.name
        %br
        = link_to 'More Info', manufacturer